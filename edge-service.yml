server:
  port: 8088

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

zuul:
  routes:
    product-service:
      path: /product/**
      serviceId: product-service
    product-dashboard-service:
      path: /dashboard/**
      serviceId: product-dashboard-service
