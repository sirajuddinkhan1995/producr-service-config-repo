server:
  port: 8088

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761

zuul:
  routes:
    product-service:
      path: /product-service/**
      service-id: product-service
    product-dashboard-service:
      path: /product-dashboard/**
      service-id: product-dashboard-service
